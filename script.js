window.addEventListener('load', () => {
    const mainSection = document.querySelector('.main-section');
    mainSection.classList.add('loaded');
  });

  window.addEventListener('load', () => {
    const mainSection = document.querySelector('.main_title');
    mainSection.classList.add('loaded');
  });